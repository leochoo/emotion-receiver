{
  "name": "receiver",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "serve": "vite preview",
    "deploy": "npm run build && firebase deploy --only hosting:receiver"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^1.0.0-next.11",
    "svelte": "^3.37.0",
    "vite": "^2.6.4"
  },
  "dependencies": {
    "@chirimen-raspi/polyfill": "latest",
    "@chirimen/pca9685": "latest",
    "firebase": "^9.2.0"
  }
  
}
